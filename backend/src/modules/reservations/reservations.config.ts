/**
 * Reservation constants. Mirrors loans.config.ts — single source of truth
 * so the seed, controller, and project-book references stay aligned.
 */
export const MAX_PENDING_RESERVATIONS_PER_READER = 5
