# Work log

Per CLAUDE.md: one entry per session, no retrofitting. Maps to project-book chapter 13.

| # | Date | Goal | What I did | Result / measurements |
|---|------|------|------------|------------------------|
| 1 | 2026-05-24 | Stand up frontend design as code | Scaffolded Vite 5 + React 18 + TS in `frontend/`. Configured Tailwind v3 with project design tokens (ink/parchment/amber palette, serif headings). Added react-router-dom. Built shared `AppShell` layout with role-aware nav. Built mock book dataset (12 titles, mixed Hebrew/English). Built shared components: `BookCover`, `BookCard`, `StarRating`. Built static mockup pages: Login, Register, Catalog (search + filters + grid), BookDetail (rating widget + similar books), Recommendations (4 hybrid recommender sections + LLM teaser), MyLoans (current/reserved/history), Librarian Dashboard (stats + chart + activity + overdue), Librarian Books / Members / Loans CRUD tables. | TypeScript build passes (`tsc -b --noEmit`, zero errors). 10 routes live. Design system tokens documented in `tailwind.config.js`. No real API yet — all data static. |
| 2 | 2026-05-24 | Shift the palette to something more vibrant; iterate on the librarian dashboard | Rewrote `tailwind.config.js` and `index.css` to a vibrant jewel-tone scheme (emerald primary, saffron, berry, coral). Replaced all 12 book cover gradients with distinct jewel-tone combos. Brightened the login left panel and the LLM recommendations teaser with multi-stop gradients and glow orbs. Rebuilt the librarian dashboard with a gradient hero band, four colored stat cards with mini sparklines, a gradient bar chart with hover tooltips, jewel-toned category bars, action-typed colored activity feed, and intensity-graded overdue badges (coral if ≥7 days late, amber if ≥4, soft if <4). | Type-check clean. Vite serves with no errors. |
| 3 | 2026-05-24 | Begin the backend — Prisma schema first | Scaffolded `backend/` with minimal `package.json`, `.env.example`, and `prisma/schema.prisma`. Modelled identity (User, RefreshToken with hashed tokens), catalog (Author, Category, Tag, Book, BookCopy with per-copy status), circulation (Loan, Reservation), and the ratings table that feeds the recommender. CUID PKs, `onDelete: Restrict` on loan FKs to preserve audit history. Wrote `docs/decisions/0001-data-model.md` covering five non-obvious modelling choices and their tradeoffs. | `prisma validate` passes. 9 entities + 5 enums. No migrations yet — schema is fixed via ADR review first. |
| 4 | 2026-05-24 | Stand up Postgres in Docker and run the first migration | Wrote `docker-compose.yml` with Postgres 16-alpine, `library`/`library`/`library` creds, healthcheck. Mapped host port to `5434` to avoid clashing with an existing host PG on `5432` (and an unknown service on `5433`). Created `backend/.env` pointed at the container. Started container, waited for healthcheck, ran `prisma migrate dev --name init`. | Migration `20260524120819_init` applied. 10 modelled tables + 2 implicit M:N junction tables (`_BookToCategory`, `_BookToTag`) present in `public`. Prisma Client v5.22 generated. Container is `healthy`. |
| 5 | 2026-05-24 | Scaffold the Express + TS backend and ship the first endpoint | Wrote strict `tsconfig.json` (ES2022, bundler resolution, ESM). Built `src/env.ts` (zod env validation, fails fast on misconfig), `src/lib/prisma.ts` (singleton with hot-reload guard), `src/utils/HttpError.ts`, `src/middleware/errorHandler.ts` (canonical `{ error: { code, message } }` shape; handles ZodError, HttpError, fallthrough), `src/middleware/validate.ts` (body/query/params zod factory), `src/modules/books/{schema,controller,routes}.ts` (list + getById with filters: q, category, language, availableOnly, sort), `src/app.ts` (helmet, cors with explicit origin allowlist, 100kb json limit, 404 sink), `src/server.ts` (graceful SIGINT/SIGTERM shutdown). Wrote `prisma/seed.ts` — idempotent upserts for 2 users (bcrypt cost 12), 12 books, 34 copies, 16 categories, 24 tags, 5 sample ratings from the reader account. | Type-check clean. Seed populates the DB cleanly. `GET /healthz` returns 200, `GET /api/v1/books` returns all 12 with category/tag arrays, `GET /api/v1/books?q=wind` filters correctly (1 result), unknown id returns 404 in the canonical error shape. |
| 6 | 2026-05-24 | Wire the frontend catalog to the live API | Installed `@tanstack/react-query` and `axios`. Wrapped the app with `QueryClientProvider` (staleTime 60s, no refetch on focus, retry 1). Added `src/api/client.ts` (axios instance, base URL from `VITE_API_URL` or `http://localhost:4000/api/v1`) and `src/api/books.ts` (typed `listBooks`/`getBook` with `CatalogBook` / `BookDetail` types). Added `src/lib/coverFor.ts` — deterministic gradient picker (12-entry jewel-tone palette, string-hashed) so books without a `cover` field still render. Updated `BookCover` and `BookCard` to accept a minimal shape and fall back to `coverFor(book.id)`. Refactored `Catalog`, `BookDetail`, `Recommendations`, and `LibrarianBooks` to use `useQuery`. Updated backend list DTO to compute and return `rating` per book so the catalog grid shows stars. Skeleton loading states on Catalog and BookDetail; error states with coral messages. | Type-check clean on both projects. Catalog reads from Postgres, "The Name of the Wind" shows ★ 5.0 (Yael's seeded rating), search filters via API query params (`?q=wind`), category chips filter via `?category=Fiction`, sort by newest works. BookDetail navigates by CUID (real DB id), similar-books strip works. MyLoans / LibrarianDashboard / LibrarianLoans still use mock data — they depend on loans which aren't API'd yet. |
| 7 | 2026-05-24 | Build the auth module (register / login / refresh / logout / me) | Installed `jsonwebtoken`, `cookie-parser`, `express-rate-limit` + types. Wrote `auth.service.ts` (bcrypt hashing cost 12, JWT access signing/verifying, 48-byte opaque refresh tokens stored as sha256 hashes, TTL parser). `auth.controller.ts` with all five endpoints, atomic refresh rotation (revoke old, issue new), uniform 401 on bad credentials (no email enumeration), 409 on duplicate email via Prisma P2002 detection. `auth.routes.ts` mounts everything with `authLimiter` (10 req / 15 min per IP) on register + login. New `middleware/auth.ts` (`requireAuth`, stateless JWT verify, attaches `{id, role}` to `req.user`) and `middleware/rbac.ts` (`requireRole(...allowed)`). Added `src/types.d.ts` augmenting Express Request with `user`. Refresh token sent as `httpOnly, SameSite=Strict, path=/api/v1/auth` cookie; `secure` flag flips on `NODE_ENV=production`. Wrote `docs/decisions/0002-authentication.md` documenting the JWT-access + opaque-refresh hybrid against three alternatives, mapped to chapter 11 security checklist. | Type-check clean. 9-scenario smoke test passes: login → access JWT + refresh cookie; `GET /auth/me` with Bearer → 200 user; no token → 401 with `{code:UNAUTHORIZED}`; refresh rotates token; using the old refresh after rotation → 401 `INVALID_REFRESH`; wrong password → 401 `INVALID_CREDENTIALS` (same shape as missing user); register new user → 201 with tokens; duplicate email → 409 `EMAIL_TAKEN`; logout → 204. |
| 8 | 2026-05-24 | Wire the frontend login flow | Built `api/tokenStore.ts` — module-level access-token holder + unauthenticated callback (token lives in memory only per ADR-0002, never localStorage). Updated `api/client.ts` with `withCredentials: true`, a request interceptor that attaches the Bearer header, and a response interceptor that on 401 (a) tries `/auth/refresh` once, (b) retries the original request, (c) notifies the AuthProvider on hard failure. `/auth/*` paths skip refresh to avoid loops. Added `api/auth.ts` with typed `login`, `register`, `refreshSession`, `logout`, `fetchMe`. Built `lib/AuthProvider.tsx` — React context with `{ user, status, login, register, logout }`; status is `loading` until boot-time `/auth/refresh + /auth/me` resolves, then `authenticated` / `unauthenticated`; clears React Query cache on logout. Built `components/RequireAuth.tsx` route guard supporting an optional `role` constraint, with a "Restoring session…" splash. Wired `pages/auth/Login.tsx` and `Register.tsx` with controlled inputs, async submits, mapped error codes (`INVALID_CREDENTIALS`, `EMAIL_TAKEN`, `TOO_MANY_REQUESTS`, `VALIDATION_ERROR`) to user-friendly strings, redirect to the `from` location after login. Rewrote `AppShell` to show real user initials, an account dropdown with Sign out, role-aware "Manage library" link only for librarians, "Sign in" button for guests, loading dot during boot. Wrapped `/recommendations`, `/my-loans`, and all `/librarian/*` routes with `RequireAuth` (with `role="LIBRARIAN"` for the librarian routes); catalog and book detail stay public for guests. | Type-check clean. Both servers up. CORS preflight returns `Allow-Origin: http://localhost:5173` with `credentials: true`. Refresh cookie is path-scoped to `/api/v1/auth` so it doesn't ride other requests. |
| 9 | 2026-05-24 | Loans module — backend, seed, and frontend rewire | Backend: `modules/loans/{config,schema,service,controller,routes}.ts`. `POST /loans` borrows the oldest AVAILABLE copy of a book inside a transaction with an `updateMany({ where: { status: 'AVAILABLE' } })` guard that resolves the two-readers-grab-the-last-copy race deterministically. `POST /loans/:id/return` flips the copy back, with a self-or-librarian authorization check. `POST /loans/:id/renew` extends 14 days, max 2 renewals, blocks renewing overdue loans. `GET /me/loans` returns the caller's loans; `GET /loans` (librarian only) returns all loans with borrower info; both DTOs carry derived `status`, `daysUntilDue`, `daysOverdue`, `fine` (2 ₪/day), `canRenew`. Constants in `loans.config.ts` (LOAN_PERIOD_DAYS=21, MAX_RENEWALS=2, MAX_ACTIVE_LOANS_PER_READER=5, FINE_PER_DAY=2). Seed: added 6 more reader users (Daniel, Maya, Eitan, Noa, Idan, Tamar) and 11 seeded loans across them — Yael's three (on-loan, overdue, returned), the dashboard's overdue trio, and recent history. Frontend: `api/loans.ts` with typed `listMyLoans`, `listLoans`, `borrowBook`, `renewLoan`, `returnLoan`. Rewrote `MyLoansPage` with live data, mutation buttons (Renew shows `(N/2)`, Return), automatic React Query invalidation. Rewrote librarian `Loans` page with status tabs, search, table that includes borrower email and per-loan fines. Updated `LibrarianDashboard` to compute stat tiles from live `?status=active`/`?status=overdue` queries, render recent activity from real loans (borrow/return verbs with relative time), and the overdue list with intensity-graded day badges. Wired the Borrow button on `BookDetail` to `POST /loans` with mapped error codes (`NO_COPY_AVAILABLE`, `LOAN_LIMIT_REACHED`) and a redirect to `/my-loans` on success; guest visitors bounce to `/login`. | Type-check clean both projects. Backend smoke-tested: Yael's 3 seeded loans render with correct status / fine; borrow → renew → return chain works; double-return → 400 `ALREADY_RETURNED`; reader hitting `/loans` → 403 `FORBIDDEN`; librarian `?status=overdue` returns 4 loans with correctly accumulated fines (Yael 18 ₪, Noa 16 ₪, Idan 10 ₪, Tamar 6 ₪). |
| 10 | 2026-05-24 | Ratings module — prerequisite for the recommender | Backend: new `middleware/auth.ts::optionalAuth` — populates `req.user` if a valid Bearer is present, passes through silently when none, returns 401 on a malformed token (so the frontend interceptor refreshes). `modules/ratings/{schema,controller,routes}.ts` with `POST /api/v1/books/:bookId/ratings` (zod-validated `value: 1..5`, upserts on the composite unique `(userId, bookId)`) and `DELETE` (204; 404 on no-op). Mounted as nested router under `books/:bookId/ratings` with `mergeParams: true`. Updated `getById` to use `optionalAuth` and include `myRating: number \| null` in the response when the caller is authed. Renamed `avgRating` field back to `rating` to match the list endpoint. Frontend: `api/ratings.ts` (`rateBook`, `deleteRating`); added `myRating` to the `BookDetail` type. Wired the BookDetail star widget to call the rate mutation on click — onSuccess invalidates `['book', id]` and `['books']` so the overall rating + ratingsCount refresh everywhere. Added "Remove rating" action; a "Sign in to rate" prompt for guests. | Type-check clean. Smoke test (7 scenarios) all pass: guest GET returns `myRating: null`; authed rate 4 → stored, then 5 → overwritten, rating average updates accordingly; value=6 → 400 `VALIDATION_ERROR` with the Zod field message; double DELETE → 404 `NOT_FOUND`; guest POST → 401. |
| 11 | 2026-05-24 | Recommendations module — the chapter-10 headline feature | Wrote `docs/decisions/0003-recommender.md` defending the hybrid (content-based + item-item CF + α blend) against pure-content, pure-CF, and matrix-factorization alternatives. `modules/recommendations/recommender.ts` — pure functions, no Prisma: `featureTokens`, `buildUserProfile` (rating-weighted sum with 3-star mean subtracted so 1★ pushes *away*), `cosineMaps`, `contentScore` ([-1,1]→[0,1]), `itemItemSim` (cosine over co-raters, null on no overlap), `cfScore` (weighted average on [1,5] scale, returns supports sorted by \|sim\| for UI "why" labels), `normalizeCf`, `hybridScore`, `alphaFor(n) = max(0.2, 1 - n/20)`. `recommendations.service.ts` loads books + all ratings + the user's ratings + recent loans in parallel, scores every candidate (book the user hasn't rated), and assembles 4 sections: because-you-liked (content desc), similar-readers (CF desc), trending (loan count last 60d), hidden-gems (ratingsCount ≤ 3, hybrid desc). Each item carries `{ score, contentScore, cfScore, why }`. `controller.ts` + `routes.ts` mounted at `GET /api/v1/recommendations` behind `requireAuth`. Expanded `prisma/seed.ts` from 5 to 31 ratings across 7 readers with deliberate co-rater overlap so CF has non-null signal for every candidate Yael could see. Frontend: `api/recommendations.ts`, rewrote `pages/reader/Recommendations.tsx` to consume live data, render the real profile α, hide sections when empty (cold-start gets only Trending), show "why" labels under each card. | Backend + frontend type-check clean. End-to-end smoke (5 scenarios): Yael (5 ratings) → profile.alpha=0.75 matches the formula; content-based section leads with Educated + A Little History of Philosophy (both share "Non-fiction" with The Pragmatic Programmer) and Hebrew memoir/literature picks (share "זיכרונות"/"ספרות ישראלית" with סיפור על אהבה וחושך); CF section has cf scores 4.33–5.00, exactly the weighted averages from the seed matrix; none of Yael's rated books leak into any section. Cold-start user (0 ratings) → alpha=1.0, only the Trending section returned, content=0.5 neutral on every item. Guest → 401 `UNAUTHORIZED`. Bogus Bearer → 401. |
| 12 | 2026-05-24 | Members module — librarian CRUD over readers | Backend: `modules/members/{schema,service,controller,routes}.ts`. `GET /api/v1/members` (librarian-only, filters `q`, `role`, `status=active-loans\|has-fines`) returns each user with derived `activeLoans`, `overdueLoans`, and `outstandingFine` (computed in JS from the live loans relation × `FINE_PER_DAY` from `loans.config`). `GET /:id`, `PATCH /:id` (firstName / lastName / role with self-demote guard), `DELETE /:id` (refuses self-delete and members with any loan history — returns canonical 409 `HAS_LOAN_HISTORY` rather than letting the Loan `onDelete: Restrict` FK throw). Frontend: `api/members.ts`, rewrote `LibrarianMembers` from a hardcoded array to a live React Query view with search + role + status filters, an Edit modal with role select + delete action, error mapping for `FORBIDDEN` / `HAS_LOAN_HISTORY`. | Backend + frontend type-check clean. Smoke (7 scenarios): list returns 10 members with matching fines from the librarian dashboard (Yael ₪18, Noa ₪16, Idan ₪10, Tamar ₪6); `?status=has-fines` filters to those 4; `?q=adler` finds Noa only; reader → 403 `FORBIDDEN`; guest → 401; PATCH renames Cold/Start successfully; Sara demoting herself → 403; DELETE no-history user → 204; DELETE Yael → 409 `HAS_LOAN_HISTORY`. |
| 13 | 2026-05-24 | Books CRUD — librarian can add, edit, delete catalog entries | Backend: extended `books.schema.ts` with `createBookBody` (title/authorName/isbn?/year?/language/blurb?/shelfCode?/categories[]/tags[]/totalCopies 1-50) and `updateBookBody` (same fields optional, no isbn change). New `books.service.ts` holds the mutations as pure functions: `createBook` upserts Author/Category/Tag by name in a single transaction, generates copy barcodes derived from the ISBN (or `BK-<id-tail>` when missing) so a partial create never leaves a book without copies; `updateBook` resets `categories.set` and `tags.set`; `deleteBook` checks `Loan.count({where: {copyId in (book's copies)}})` and surfaces `HAS_LOAN_HISTORY` 409 instead of relying on the FK `Restrict`. Controllers + routes added under the existing `books.routes.ts` behind `requireRole('LIBRARIAN')`. Frontend: extended `api/books.ts` with `createBook`/`updateBook`/`deleteBook`; rewrote `LibrarianBooks` with an Add Book button + a shared modal `BookEditor` (comma-separated categories/tags inputs, language select, year/initial-copies coerced to numbers), per-row Edit and Delete actions with confirmation. | Both projects type-check. Smoke (6 scenarios): reader → 403 on POST; librarian creates "Sea of Tranquility" with 2 copies + 2 categories + 2 tags via name (no taxonomy IDs); duplicate ISBN → 409 `ISBN_TAKEN`; PATCH updates year and adds a tag (set semantics correctly replaces the array); DELETE of the new book (no loans) → 204; DELETE of "The Name of the Wind" (has loan history) → 409 `HAS_LOAN_HISTORY`. |
| 14 | 2026-05-24 | Reservations module — queue for unavailable books, auto-fulfil on return | Backend: `modules/reservations/{config,schema,service,controller,routes}.ts`. `POST /api/v1/books/:bookId/reservations` (reader) creates a PENDING reservation. Refuses with 409 `COPIES_AVAILABLE` when an AVAILABLE copy exists ("borrow instead"), 409 `ALREADY_RESERVED` when the reader has a PENDING reservation for the same book, and 403 `RESERVATION_LIMIT_REACHED` past `MAX_PENDING_RESERVATIONS_PER_READER = 5`. `DELETE /api/v1/reservations/:id` cancels with self-only check. `GET /api/v1/me/reservations?status=active\|history\|all` and `GET /api/v1/reservations` (librarian, all PENDING). DTOs carry a 1-based `queuePosition` derived from `createdAt asc` among PENDING for the same `bookId` — computed in one extra query and joined in JS. Critical integration: `loans.service.returnLoan` now calls `fulfillNextInQueue(tx, bookId)` inside its existing transaction, so a copy flipping to AVAILABLE and the head of the queue transitioning to FULFILLED are atomic. Frontend: `api/reservations.ts`; `BookDetail` Place-on-hold button when `available === 0` calls the reserve mutation with error mapping (`ALREADY_RESERVED`, `COPIES_AVAILABLE`, `RESERVATION_LIMIT_REACHED`); `MyLoansPage` gained Ready-to-pick-up and Active-holds sections with cancel and "Borrow now" actions. | Both projects type-check. Smoke (9 scenarios): with The Name of the Wind at 0/2, Daniel borrows the last copy; Maya reserves → queue position 1; Noa reserves → queue position 2; Maya re-reserves → 409 `ALREADY_RESERVED`; Yael returns her seeded loan → Maya's reservation flips to FULFILLED (fulfilledAt set) and Noa moves from queue position 2 → 1 in the same transaction; Noa cancels → 204; Daniel returns the second copy → 200 (Noa already cancelled, fulfilment is a no-op). |
| 15 | 2026-05-24 | Fix the refresh-rotation race | Found during the prior browser verification: React 18 StrictMode runs `useEffect` twice on mount, so the `AuthProvider` boot fired two parallel `POST /auth/refresh` calls with the same refresh cookie. Per ADR-0002 the server rotates the refresh hash on first hit and revokes the old one, so the second call always 401s with `INVALID_REFRESH` and trips `tokenStore.notifyUnauthenticated()` — kicking the user to `/login` on a valid session. Same shape if the axios 401 interceptor's refresh races the `AuthProvider`'s. Fix: new `frontend/src/api/refresh.ts` holds a single-flight `inflight` promise; both `auth.ts::refreshSession` and the `client.ts` 401 interceptor now go through it. Concurrent callers share the same promise and the same successful result; only one network request and one cookie rotation per burst. | Type-check clean. Playwright verification: every page load now fires exactly **1** `/auth/refresh` (was 2 under StrictMode) across `/login`, `/`, `/recommendations`, `/my-loans`. The Borrow flow that previously bounced to `/login` during navigation now lands on `/my-loans` cleanly with 0 extra refresh calls. |
| 16 | 2026-05-25 | Server-side defence for cross-tab refresh races + chapter 14 test plan | Reviewed an uncommitted change to `backend/src/modules/auth/auth.controller.ts` that swapped the refresh-rotation `update` for a CAS-style `updateMany({ where: { id, revokedAt: null }, ... })`. Found a follow-on bug: the loser path called `clearRefreshCookie(res)`, which would race-overwrite the winner's freshly-set cookie in the same response batch and log both tabs out. Fixed by: (a) loser returns 401 with new code `REFRESH_RACE` (distinct from `INVALID_REFRESH`) and **does not** clear the cookie; (b) updated JSDoc to spell out the cookie-jar invariant. Wired the frontend retry: `frontend/src/api/refresh.ts` now wraps a private `attempt()` and, on `REFRESH_RACE`, retries once — single-flight `inflight` still in force, so all coalesced callers (AuthProvider boot + axios 401 interceptor) inherit the retry transparently. Then drafted `docs/book/14-test-plan.md` to satisfy MAHAT chapter 14: 57 full-flow rows (each citing the worklog entry where it was manually smoke-tested) and 32 unit rows over the recommender pure functions, auth.service, the schemas, and the derivation helpers in loans/reservations/members. Included an automation roadmap (Jest+Supertest, DATABASE_URL_TEST against a separate Postgres DB, `Promise.all` parallel-refresh as the first automated case) and defence notes for the oral. | `tsc -b --noEmit` clean both projects. No new runtime tests yet — that's phase 1 of the roadmap. The end-to-end race fix (server CAS + client retry) is in place but the F7 row is honestly marked **Planned** until automation is wired. |
| 17 | 2026-06-01 | Automate F7 (parallel-refresh race) — and fix the bug it found | Stood up the Jest+Supertest harness (phase 1 of the chapter-14 roadmap): added `jest`, `ts-jest`, `supertest`, `cross-env`; `jest.config.js` runs the `ts-jest` ESM preset under `--experimental-vm-modules` with a `moduleNameMapper` that strips `.js` specifiers (this project is native ESM); `tests/setup-env.ts` redirects Prisma to a dedicated `library_test` DB before any app module loads; `tests/globalSetup.ts` runs `prisma migrate deploy`; `tsconfig.jest.json` widens `rootDir` for the `tests/` tree. Wrote `tests/auth.refresh-race.test.ts` driving the race with `Promise.all` against real Postgres. **The test caught a real bug**: a characterization loop (20 races, tokens seeded via Prisma to dodge the auth rate limiter) measured the loser returning `INVALID_REFRESH` — which calls `clearRefreshCookie` — on ~5% of races, not the intended `REFRESH_RACE`. Root cause: the validity check short-circuited on the initial read's `revokedAt`, so a loser whose read landed after the winner's commit took the cookie-clearing path and logged the cross-tab session out — the exact bug #16 meant to kill, just in a narrower window. Fix (chosen over a grace-window heuristic): made the CAS the **sole** arbiter — the early guard now only rejects *unknown/expired* tokens (→ `INVALID_REFRESH` + clear); any *known* token the CAS finds already revoked → `REFRESH_RACE`, no clear. Updated the JSDoc, ADR-0002 (race tradeoff + explicit "no token-family reuse detection, by scope" note), and the F6/F7 rows + roadmap in `14-test-plan.md`. F6 semantics intentionally shift: reuse of a rotated-away (revoked-but-unexpired) token is now `REFRESH_RACE`, indistinguishable from a race loser. | `tsc -b --noEmit` clean both projects; `tsc -p tsconfig.jest.json` clean. `npm test` green: 3 specs, the race spec runs 8 rounds/run. Ran the suite 4× (32 total races) — loser is `REFRESH_RACE` and never clears the cookie every time (pre-fix this flaked ~1–2 times in 32). F7 moves from **Planned** to **Pass (automated)**. |
| 18 | 2026-06-01 | Root run scripts + README | Added a root `package.json` (private, no runtime deps) that orchestrates the monorepo via npm scripts delegating to the two workspaces with `npm --prefix`: `dev` runs API + SPA together through `concurrently` (`[api]`/`[web]` prefixed output, `-k` so Ctrl+C or a crash stops both), plus `dev:backend`/`dev:frontend`, `db:up` (`docker compose up -d --wait` — blocks on the healthcheck), `db:down`, `db:logs`, `db:deploy`, `db:seed`, `test`, `build`, `install:all`, and a one-shot `setup`. Added a non-interactive `db:deploy` (`prisma migrate deploy`) to `backend/package.json` so `setup` never prompts. `concurrently` is the only new dependency — dev-only, chosen over a hand-rolled launcher because it does proper process-tree teardown on Ctrl+C (the hand-rolled version leaves orphaned node processes on Windows). Wrote the first `README.md` (prereqs, first-time setup, the run table, ports, seeded logins, test-DB note). | `npm run db:up` → container healthy. `npm run dev` smoke-tested: both servers boot in ~1.5s, `GET /healthz` → `{"status":"ok"}` (200) and the Vite dev server answers 200 on :5173; concurrently's prefixed output renders correctly; Ctrl+C/tree-kill stops both. |
| 19 | 2026-06-15 | Drop the fines feature; make returns librarian-only | Two scope changes. **Fines removed entirely**: deleted `FINE_PER_DAY` from `loans.config.ts`, dropped the `fine` field + its computation from `loans.service.toLoanDTO`, and removed the `outstandingFine` column + `has-fines` filter from `members.service`/`members.schema` (the `status` enum is now just `active-loans`). Frontend: removed the `fine`/`outstandingFine` fields from `api/loans.ts` and `api/members.ts`, the "Outstanding fines" stat and per-loan fine text from reader `MyLoans`, the **Fine** table column from librarian `Loans`, the **Fines** column + "Has fines" filter from librarian `Members`, and the in-fines deltas/labels from the `Dashboard` (overdue tiles now read "needs follow-up" / "{n} to follow up" / "{n} days overdue"). **Returns are now librarian-only**: added `requireRole('LIBRARIAN')` to `POST /loans/:id/return`, simplified `loans.service.returnLoan(loanId)` to drop the borrower/role args and the self-or-librarian check (the route gate is now the sole arbiter), updated the controller call site, and removed the Return button + its mutation from the reader `MyLoans` page (readers keep Renew; librarians return at the desk via the Loans page). Updated `14-test-plan.md` rows F34/F35/F43/F57 and marked U27/U32 removed. | `tsc --noEmit` clean on both projects. No automated tests broke (only the refresh-race spec exists). Grep confirms zero remaining `FINE_PER_DAY`/`outstandingFine`/`has-fines`/`.fine` references in `src`. |
